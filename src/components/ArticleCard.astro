---
interface Props {
  href: string;
  title: string;
  description?: string;
  lastUpdated?: Date;
  category?: string;
  cluster?: string;
  readingTime?: string;
}

const { href, title, description, lastUpdated, category, cluster, readingTime } = Astro.props;
const fmtDate = (d: Date) =>
  d.toLocaleDateString("en-US", { year: "numeric", month: "short" });
const eyebrow = cluster ?? category;
---

<a
  href={href}
  class="block p-6 no-underline border transition-all hover:-translate-y-0.5 h-full"
  style="background: var(--color-surface); border-color: var(--color-border); border-radius: var(--radius-lg);"
>
  {eyebrow && <div class="eyebrow mb-3">{eyebrow}</div>}
  <h3 class="text-xl leading-snug mb-2">{title}</h3>
  {description && (
    <p class="text-sm leading-relaxed line-clamp-3" style="color: var(--color-text-muted);">
      {description}
    </p>
  )}
  <div class="mt-4 font-sans-ui text-xs flex gap-3" style="color: var(--color-text-subtle);">
    {lastUpdated && <time datetime={lastUpdated.toISOString()}>Updated {fmtDate(lastUpdated)}</time>}
    {readingTime && (
      <>
        <span aria-hidden="true">·</span>
        <span>{readingTime}</span>
      </>
    )}
  </div>
</a>
